{
    "watch": [
        "src",
        "schema"
    ],
    "ext": "ts,json,grahpql",
    "ignore": [
        "src/**/*.spec.ts"
    ],
    "exec": "graphql-codegen --config codegen.yml && ts-node ./src/server.ts",
    "env": {
        "NODE_OPTIONS": "--enable-source-maps"
    }
}